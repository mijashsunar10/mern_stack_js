 function nyName(a,b)
 {
    let name = `My name is ${a} ${b}`;
    return name;

 }
    console.log(nyName("Mijash","Kumar"));

    function name1(c,d)
    {
        let fn = c + " " + d;
        return fn;
    }

    let finalans = name1(7,7);
    console.log(finalans);

      function name2(c,d)
    {
        let fn = c + d;
        return fn;
    }

    let finalans1 = name2(7,7);
    console.log(finalans1);